#compdef millennium

_millennium_theme_names() {
  local -a themes
  local home="${HOME:-}"
  local d skins
  for skins in \
    "${home}/.local/share/Steam/steamui/skins" \
    "${home}/.steam/steam/steamui/skins" \
    "${home}/.steam/root/steamui/skins" \
    "${home}/.var/app/com.valvesoftware.Steam/.local/share/Steam/steamui/skins" \
    "${home}/Library/Application Support/Steam/steamui/skins"; do
    [[ -d "$skins" ]] || continue
    for d in "$skins"/*(/N); do
      themes+=("${d:t}")
    done
    break
  done
  _describe 'theme' themes
}

_millennium_backup_ids() {
  local -a backups
  local d
  for d in /usr/lib/millennium.bak_*(/N); do
    backups+=("${d:t}")
  done
  [[ -d /usr/lib/millennium.bak ]] && backups+=("millennium.bak")
  backups+=("list")
  _describe 'backup' backups
}

_millennium_helpers() {
  local curcontext="$curcontext" state line
  typeset -A opt_args

  local -a cmds
  # @@cli-contract:dispatcher.commands@@
  cmds=(
    'diag:Run diagnostics'
    'doctor:Alias for diag doctor'
    'upgrade:Upgrade / install Millennium'
    'schedule:Manage auto-update scheduler'
    'theme:Manage skins/themes'
    'config:Manage Millennium client settings without the Steam UI'
    'injection:Enable or disable Millennium injection'
    'repair:Repair hooks, runtime permissions, and ownership'
    'purge:Uninstall Millennium'
    'mcp:Run / register MCP server'
    'install:Install helpers on this machine'
    'uninstall:Remove helpers installed by millennium install'
    'help:Show help'
  )
  # @@/cli-contract:dispatcher.commands@@
  _arguments -C \
    '1:command:((${cmds}))' \
    '*::arg:->args'
  case $state in
    (args)
      case $line[1] in
        (diag|doctor)
          _arguments \
            '1:command:(doctor logs)' \
            '--json[Output diagnostics report in structured JSON format]' \
            {-f,--fix}'[Alias for the doctor command]' \
            '--force[Force all doctor repairs]' \
            {-l,--follow}'[Follow real-time log output]' \
            {-y,--yes}'[Skip confirmation when doctor closes Steam]' \
            {-d,--dry-run}'[Simulate doctor repairs]' \
            {-q,--quiet}'[Suppress informational output]' \
            {-s,--share}'[Upload diagnostic report]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (upgrade)
          _arguments \
            {-c,--channel}'[Update channel]:channel:(stable beta main)' \
            '--stable[Alias for --channel stable]' \
            '--beta[Alias for --channel beta]' \
            '--main[Alias for --channel main]' \
            {-r,--rollback}'[Roll back to a previous backup]:backup:_millennium_backup_ids' \
            '--file[Install from a local archive]:file:_files' \
            '--sha256[Expected SHA-256 of --file archive]' \
            '--insecure-skip-verify[Skip archive checksum verification]' \
            '--all-users[Unix: apply install for all users]' \
            {-f,--force}'[Force reinstall]' \
            {-y,--yes}'[Skip confirmation when closing Steam]' \
            {-d,--dry-run}'[Simulation mode]' \
            {-q,--quiet}'[Suppress informational output]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (schedule)
          _arguments \
            '1:command:(enable disable status setup config)' \
            {-c,--cron}'[Force use of crontab instead of systemd]' \
            --system'[Linux: force systemd system units]' \
            --user'[Linux: force systemd user units]' \
            {-d,--dry-run}'[Simulation mode]' \
            {-q,--quiet}'[Suppress informational output]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (theme)
          _arguments \
            '1:command:(list install update remove)' \
            '2:theme:_millennium_theme_names' \
            {-a,--all}'[Update all themes]' \
            '--json[Output list as JSON]' \
            {-y,--yes}'[Skip confirmation when removing]' \
            {-d,--dry-run}'[Simulation mode]' \
            {-q,--quiet}'[Suppress informational output]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (config)
          _arguments \
            '1:command:(show plugins themes errors enable disable theme disable-theme disable-errors)' \
            '*:name:' \
            '--json[Output structured JSON]' \
            {-d,--dry-run}'[Show changes without writing]' \
            {-q,--quiet}'[Suppress mutation confirmation]' \
            {-y,--yes}'[Confirm disabling flagged components]' \
            --plugins-only'[Disable only flagged plugins]' \
            --themes-only'[Disable only the flagged active theme]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (injection)
          _arguments \
            '1:command:(status disable enable)' \
            {-y,--yes}'[Skip disable confirmation]' \
            {-d,--dry-run}'[Simulation mode]' \
            {-q,--quiet}'[Suppress informational output]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (repair)
          _arguments \
            {-s,--skip-theme}'[Skip theme refresh]' \
            {-y,--yes}'[Skip confirmation when closing Steam]' \
            {-d,--dry-run}'[Simulation mode]' \
            {-q,--quiet}'[Suppress informational output]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (purge)
          _arguments \
            {-y,--yes}'[Skip confirmation]' \
            {-d,--dry-run}'[Simulation mode]' \
            {-q,--quiet}'[Suppress informational output]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (mcp)
          _arguments \
            {-r,--register}'[Register MCP server with AI clients]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
        (install|uninstall)
          _arguments \
            '--track[Helpers install track]:track:(release main tag checkout)' \
            '--tag[Install a specific release tag]' \
            '--allow-unsigned-main[Allow tip-of-main unsigned archive]' \
            '--prefix[Binary install directory prefix]' \
            '--target-dir[Binary install directory]' \
            '--lib-dir[Unix lib directory]' \
            '--source-root[Checkout or extracted archive root]' \
            '--skip-wizard[Do not launch schedule setup]' \
            {-p,--purge}'[Also purge Millennium client (uninstall)]' \
            {-d,--dry-run}'[Simulation mode]' \
            {-f,--force}'[Force reinstall]' \
            {-V,--version}'[Show version information]' \
            {-h,--help}'[Show help message]'
          ;;
      esac
      ;;
  esac
}

_millennium_helpers "$@"
