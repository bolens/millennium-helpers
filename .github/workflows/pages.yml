name: Pages

on:
  pull_request:
    paths:
      - site/**
      - CHANGELOG.md
      - scripts/render-changelog.py
      - docs/architecture/**
      - scripts/ci/build-pages-site.py
      - scripts/ci/check-pages-site.py
      - VERSION
      - .github/workflows/pages.yml
  push:
    branches: [main]
    paths:
      - site/**
      - CHANGELOG.md
      - scripts/render-changelog.py
      - docs/architecture/**
      - scripts/ci/build-pages-site.py
      - scripts/ci/check-pages-site.py
      - VERSION
      - .github/workflows/pages.yml
  release:
    types: [published]
  workflow_dispatch:

permissions: {}


concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - name: Verify generated changelog
        run: python3 scripts/render-changelog.py --name "Millennium Helpers" --base-url https://bolens.github.io/millennium-helpers/ --accent "#78dce8" --social-image assets/social-card.png --favicon assets/favicon.png --docs-shell --output site/changelog/index.html
      - name: Validate site source
        run: python3 scripts/ci/check-pages-site.py --site site --architecture docs/architecture/millennium-helpers.html
      - name: Build versioned preview
        run: |
          version="v$(tr -d '\r\n' < VERSION)"
          python3 scripts/ci/build-pages-site.py --architecture docs/architecture/millennium-helpers.html --architecture-name millennium-helpers.html --version "$version"
      - name: Check rendered accessibility
        run: |
          python3 -m http.server 4173 --directory _site &
          npx --yes lighthouse@12.8.2 http://127.0.0.1:4173/ --only-categories=accessibility --chrome-flags='--headless --no-sandbox' --preset=desktop --output=json --output-path=/tmp/lighthouse.json --quiet
          python3 -c "import json; score=json.load(open('/tmp/lighthouse.json'))['categories']['accessibility']['score']; print(f'accessibility={score:.0%}'); assert score == 1"

  deploy:
    permissions:
      contents: read
      id-token: write
      pages: write
    if: github.event_name != 'pull_request'
    needs: validate
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false
      - uses: actions/configure-pages@45bfe0192ca1faeb007ade9deae92b16b8254a0d # v6
      - name: Assemble versioned site
        run: |
          version="v$(tr -d '\r\n' < VERSION)"
          python3 scripts/ci/build-pages-site.py --architecture docs/architecture/millennium-helpers.html --architecture-name millennium-helpers.html --version "$version"
      - uses: actions/upload-pages-artifact@fc324d3547104276b827a68afc52ff2a11cc49c9 # v5
        with:
          path: _site
          include-hidden-files: true
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@cd2ce8fcbc39b97be8ca5fce6e763baed58fa128 # v5
