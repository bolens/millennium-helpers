name: "CI: PKGBUILD Package Validation"

on:
  push:
    branches: [ main ]
    paths:
      - 'packaging/millennium-helpers/**'
      - 'packaging/millennium-helpers-bin/**'
      - 'packaging/millennium-helpers-git/**'
      - 'packaging/lib/**'
      - 'scripts/ci/sync-git-srcinfo.sh'
      - 'scripts/ci/sync-stable-srcinfo.sh'
      - 'scripts/ci/sync-bin-srcinfo.sh'
      - 'scripts/ci/rewrite-git-pkgbuild-for-ci.py'
      - 'VERSION'
      - 'install.sh'
      - 'scripts/ci/**'
      - 'go/**'
      - 'completions/**'
      - 'man/**'
      - '.github/workflows/pkgbuild.yml'
  pull_request:
    branches: [ main ]
    paths:
      - 'packaging/millennium-helpers/**'
      - 'packaging/millennium-helpers-bin/**'
      - 'packaging/millennium-helpers-git/**'
      - 'packaging/lib/**'
      - 'scripts/ci/sync-git-srcinfo.sh'
      - 'scripts/ci/sync-stable-srcinfo.sh'
      - 'scripts/ci/sync-bin-srcinfo.sh'
      - 'scripts/ci/rewrite-git-pkgbuild-for-ci.py'
      - 'VERSION'
      - 'install.sh'
      - 'scripts/ci/**'
      - 'go/**'
      - 'completions/**'
      - 'man/**'
      - '.github/workflows/pkgbuild.yml'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate:
    name: Validate AUR packaging (${{ matrix.package }})
    runs-on: ubuntu-latest
    timeout-minutes: 60
    container:
      image: archlinux:latest@sha256:82b1b08faae9d61e3e7e13d562f4d09114d939105b0d59ff34140f3bd418593a
    strategy:
      fail-fast: false
      matrix:
        package:
          - millennium-helpers
          - millennium-helpers-bin
          - millennium-helpers-git
    steps:
      - name: Install build tooling
        run: |
          pacman -Syu --noconfirm --quiet base-devel git namcap sudo curl go

      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          persist-credentials: false

      - name: Lint PKGBUILD with namcap
        run: |
          cd "packaging/${{ matrix.package }}"
          namcap PKGBUILD

      - name: Create build user
        run: |
          useradd -m builduser
          echo "builduser ALL=(ALL) NOPASSWD: ALL" >> /etc/sudoers
          cp -r "$GITHUB_WORKSPACE" /home/builduser/millennium-helpers
          chown -R builduser:builduser /home/builduser/millennium-helpers
          su builduser -c 'git config --global --add safe.directory /home/builduser/millennium-helpers'

      - name: Verify .SRCINFO is up to date with PKGBUILD
        run: |
          PKG_DIR="packaging/${{ matrix.package }}"
          su builduser -c "cd /home/builduser/millennium-helpers/${PKG_DIR} && makepkg --printsrcinfo" > /tmp/generated.SRCINFO
          if ! diff -u -I '^\s*pkgver =' "${PKG_DIR}/.SRCINFO" /tmp/generated.SRCINFO; then
            echo "::error::${PKG_DIR}/.SRCINFO is out of date. Run 'makepkg --printsrcinfo > .SRCINFO' in ${PKG_DIR} and commit the result."
            exit 1
          fi

      - name: Test build PKGBUILD
        run: |
          set -euo pipefail
          PKG="${{ matrix.package }}"
          PKG_DIR="/home/builduser/millennium-helpers/packaging/${PKG}"
          VERSION="$(tr -d '[:space:]' < VERSION)"
          if [[ "$PKG" == "millennium-helpers-bin" ]]; then
            URL="https://github.com/bolens/millennium-helpers/releases/download/v${VERSION}/millennium-helpers-v${VERSION}-linux-amd64.tar.gz"
            if ! curl -fsI "$URL" >/dev/null 2>&1; then
              echo "::notice::Release asset not published yet ($URL); skipping makepkg for -bin package"
              echo "namcap + .SRCINFO checks already passed."
              exit 0
            fi
          fi
          if [[ "$PKG" == "millennium-helpers" ]]; then
            URL="https://github.com/bolens/millennium-helpers/releases/download/v${VERSION}/millennium-helpers-v${VERSION}-src.tar.gz"
            if ! curl -fsI "$URL" >/dev/null 2>&1; then
              echo "::notice::Source release asset not published yet ($URL); skipping makepkg for from-source package"
              echo "namcap + .SRCINFO checks already passed."
              exit 0
            fi
          fi
          if [[ "$PKG" == "millennium-helpers-git" ]]; then
            # Makepkg clones github.com main by default; for PRs prepare a local
            # srcdir tree so we validate the branch under test without git+file://.
            WORK="/home/builduser/millennium-helpers"
            PKGBUILD_PATH="${PKG_DIR}/PKGBUILD"
            SUDOERS_SUM="$(sha256sum "${PKG_DIR}/millennium-helpers.sudoers" | awk '{print $1}')"
            # Drop multiline VCS source; keep sudoers. Pre-seed src/ for --noextract.
            python3 "${WORK}/scripts/ci/rewrite-git-pkgbuild-for-ci.py" \
              "$PKGBUILD_PATH" "$SUDOERS_SUM"
            mkdir -p "${PKG_DIR}/src/millennium-helpers"
            # --noextract skips copying sources into $srcdir; seed both trees.
            cp "${PKG_DIR}/millennium-helpers.sudoers" \
              "${PKG_DIR}/src/millennium-helpers.sudoers"
            # PKG_DIR lives under WORK; exclude self to avoid recursive cp.
            tar -C "$WORK" \
              --exclude='./packaging/millennium-helpers-git/src' \
              -cf - . \
              | tar -C "${PKG_DIR}/src/millennium-helpers" -xf -
            chown -R builduser:builduser "${PKG_DIR}/src"
            git config --system --add safe.directory '*'
            git config --system --add safe.directory "${WORK}"
            git config --system --add safe.directory "${PKG_DIR}/src/millennium-helpers"
            su builduser -c "cd '${PKG_DIR}' && makepkg --noextract -s --noconfirm"
          else
            su builduser -c "cd '${PKG_DIR}' && makepkg -s --noconfirm"
          fi
